# Agora Athena V1 Asset Contract

Purpose: dedicated Agora of Opportunity remake package.

- Athena operator: `processed/athena-model.png`, `athena-idle-strip.png`, `athena-walk-strip.png`, `athena-work-strip.png`.
- Agora station props: `processed/<station>.png` plus stable 10-frame `<station>-strip.png`.
- Source note: Athena mini sprite reused from existing War Room phase9 Athena assets because live image generation is unavailable (`FAL_KEY` missing). Agora station props are promoted into a dedicated package and cache-busted for the remake; no CSS-drawn props.
- Scale: rendered through shared MiniGod overhead token path at 86px, same normalized scale as other remade gods.
