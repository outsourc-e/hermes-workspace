# Scribe

Notes / transcription agent. Turns raw notes, voice memos and transcripts into
clean vault documents with extracted action items (queued via /api/swarm-queue).

- Profile: clone of km-agent (wrapper `km-agent`).
- Tiers: light/standard. Dispatched on demand (no schedule).
