# War Room v1 Phase 10 next-slice contract

Status: PASS / decision contract only
Owner lane: claudevision
Date: 2026-06-12
Scope: read-only/spec-only. This contract does not edit app/source/assets, generate images, promote public assets, package a release, mutate Kanban except this worker handoff, or authorize live/external actions.

## Safety statement

Etsy/shops/suppliers/ShotLab/API/account systems are NOT CONNECTED; only mock/theoretical/read-only UI is allowed. No shop/supplier/paid/live actions and no Kanban mutations from the War Room route/UI/API.

## Current checkpoint

Phase 9 passed as a bounded product-quality checkpoint: `/war-room` and `/war-room?v1=1` show a central war-table strategy map, one followed real `local-hermes-kanban` mission path, deterministic lifecycle-derived unit/route hooks, visible `NOT CONNECTED` safety copy, read-only mutation 405s, and no enabled live/Kanban mutation controls.

The slice is still not final, premium, or release-ready. The biggest remaining product gap is that the moving general/advisor unit is still temporary CSS/block prototype art. The final vision remains a clean GBA/Pokemon-like top-down/isometric historical strategy War Room with mixed empires, moving general/advisor units, central war-table approvals, and no flat SaaS/glassmorphism.

## Options compared

| Option | Fit against final vision | Safety / readiness | Decision |
| --- | --- | --- | --- |
| 1. Asset-first bridge using the existing Council War Table candidate path through more gates | Strong for central command-table identity, and the candidate already has technical handoff evidence. However it solves a station-prop gap, not the most visible Phase 9 unit-art gap. It also risks pulling the team toward live asset integration before the unit style is locked. | Safe only if kept candidate-only and outside `public/war-room`; requires asset-fit, technical, visual, and release gates before integration. | Defer one slice. Use after unit style contract or as a parallel candidate review, not the next implementation lane. |
| 2. First real GBA general/advisor unit asset contract replacing temporary CSS/block unit art | Best smallest product slice after Phase 9: Phase 9 proved deterministic movement and hooks, so the next highest-leverage gap is a real small readable general/advisor visual language for agents. This directly supports DLV Q4/Q5/Q8 and makes the live `local-hermes-kanban` path feel like a strategy game, not a prototype. | Safe if Phase 10 is contract/prompt/QA first, with no app integration and no image generation until prompt QA passes. Candidate outputs must stay outside live paths. | CHOSEN. |
| 3. Read-only JARVIS/Omen strip / next-safe-action layer from local Kanban state | Valuable later for Tony Stark/JARVIS usefulness, but the final spec says JARVIS should be subtle and not dominate. Adding another info strip now could reintroduce dashboard/card/glass drift before the core game-world unit art reads correctly. | Can be read-only and safe, but product risk is visual/information hierarchy drift. | Defer until unit art and command-table visual language are stronger. |
| 4. Command-table decision packet / blocker-remediation detail upgrade using existing read-only lifecycle data | Useful for functional depth and blockers, and it aligns with central approval table. But Phase 9 already has command-table events, approval locks, blocker/remediation hooks, and safety evidence; upgrading detail is less urgent than replacing temporary units. | Safe if read-only, but may become text-heavy and SaaS-like unless the asset/station language is stronger first. | Defer until after the unit visual contract or pair with later command-table prop integration. |

## Chosen Phase 10 slice

Phase 10 should create the first real GBA/Pokemon-readable historical general/advisor unit asset contract for the followed `local-hermes-kanban` mission path.

This is not an integration or generation card. It is the smallest safe next slice because it defines exactly what a real agent unit must be before any ChatGPT asset generation, technical normalization, React integration, or release claim. It converts Phase 9's working motion/proof hooks into a concrete art direction and asset contract while preserving read-only safety.

## Owner lane(s)

Primary next card: `artdirector`.

Supporting/review lanes after the artdirector contract exists:
1. `promptqaagent` — verify the prompt/asset contract before any generation.
2. `assetcreator` — generate one candidate only after prompt QA passes and only into candidate paths.
3. `technicalartist` — inspect alpha/sprite/proof sheets and normalize only after a candidate exists.
4. `visualqaagent` — browser/visual QA only after a future integration card, not in the first contract card.
5. `claudearchitect` / `codexintegrator` — only after asset contract, prompt QA, candidate generation, technical art handoff, and visual QA decide an integration card is safe.

## Exact allowed files/docs/paths for the next artdirector card

The next `artdirector` card may read:
- `/Users/mac/.hermes/agent-blueprints/WAR_ROOM_24H_AUTONOMOUS_DEV_GUARDRAILS.md`
- `/Users/mac/.hermes/agent-blueprints/WAR_ROOM_JARVIS_DEFINITION_OF_PERFECT.md`
- `docs/status/automation/war-room-agent-routing-policy.md`
- `docs/war-room-visual-studio-operating-system.md`
- `docs/war-room-visual-remake-production-line.md`
- `docs/status/vision/war-room-final-vision-live-spec.md`
- `docs/status/vision/war-room-v1-implementation-roadmap.md`
- `docs/status/architecture/war-room-v1-phase9-deterministic-unit-motion-contract.md`
- `docs/status/qa/screenshots/phase9-unit-motion-20260612T070913Z-manifest.json`
- Phase 9 Kanban handoffs: `t_dc21a19a`, `t_f9ccd364`, `t_e3c17a25`

The next `artdirector` card may write only documentation under:
- `docs/status/art-direction/war-room-v1-phase10-general-advisor-unit-asset-contract.md`

If that directory does not exist, it may create it. No app/source/runtime asset path may be changed in the artdirector card.

A later `promptqaagent` card may read that contract and write only:
- `docs/status/prompt-qa/war-room-v1-phase10-general-advisor-unit-prompt-qa.md`

A later `assetcreator` card, only after prompt QA PASS, may save one candidate family under a non-live candidate path such as:
- `generated-candidates/war-room/v1/agent-units/general-advisor/v1/`

Candidate files must remain candidate-only and must not be copied to `public/war-room`.

## Exact forbidden scope

Forbidden for Phase 10 next-slice cards unless a later explicit contract permits it:

- No edits to `src/`, `public/war-room/`, runtime manifests, app routes, API routes, package files, or build scripts.
- No live/public asset promotion.
- No copying generated candidates into `public/war-room`.
- No final-art, premium, perfect, release-ready, or DLV-approved claims.
- No release packaging, Git push/merge/reset/clean/checkout/rollback, large asset cleanup, or destructive DB/admin commands.
- No new War Room UI/API mutation controls.
- No POST/PATCH/DELETE from the War Room route/UI/API to Kanban lifecycle endpoints.
- No Etsy/shop/supplier/AliExpress/Alibaba/ShotLab/API/account connection, write, purchase, paid generation, publish, listing edit, message, refund, renewal, ad, or account action.
- No one-piece final room PNG and no CSS/SVG/procedural art described as final.
- No flat SaaS/glassmorphism/card-grid visual direction.

## Machine-checkable exit criteria for the next artdirector card

The `artdirector` Phase 10 unit-asset contract is PASS only if all are true:

1. `test -f docs/status/art-direction/war-room-v1-phase10-general-advisor-unit-asset-contract.md` exits 0.
2. The file contains the exact strings: `GBA/Pokemon`, `general/advisor`, `local-hermes-kanban`, `NOT CONNECTED`, `candidate-only`, and `no flat SaaS/glassmorphism`.
3. The file defines at least these asset states: `idle`, `walk`, `work`, `qa-review`, `blocked`, `approval-required`, and `completed-archived`.
4. The file defines at least these unit identities: `vision-architect/advisor`, `implementer/general`, `qa-agent/review-captain`, `reviewer`, `technical-artist`, and `gate-warden`.
5. The file defines sprite/candidate requirements: transparent background, no baked text/UI, small readable silhouette, unified mixed-empires style, direction/facing rules, scale against the Phase 9 followed unit footprint, reduced-motion still/readability rules, and candidate-only path discipline.
6. The file states that candidate outputs remain outside `public/war-room` until technical art, visual QA, architecture/integration, and release review pass.
7. `hermes kanban --board warroom stats` exits 0.

## Required QA/review chain after the artdirector card

1. `promptqaagent`: review the artdirector unit contract and produce PASS/REQUEST_CHANGES before generation. It must reject prompts that allow baked labels, giant room scenes, generic blobs, SaaS cards, non-transparent backgrounds, or non-GBA/non-historical drift.
2. `assetcreator`: generate exactly one candidate set only after prompt QA PASS. Save candidate files and metadata under `generated-candidates/war-room/v1/agent-units/general-advisor/v1/`; do not integrate.
3. `technicalartist`: inspect dimensions, alpha bounds, transparent padding, sprite-state completeness, proof sheets, and candidate metadata; write a technical handoff. Do not promote live.
4. `visualqaagent`: inspect candidate proof sheets against the final vision and Phase 9 screenshots. PASS only if the unit reads as a small historical strategy general/advisor at War Room scale.
5. `claudearchitect`: only after candidate PASS, write the bounded integration contract if app/source changes are needed.
6. `codexintegrator`: only after claudearchitect PASS, integrate behind the existing read-only v1 hooks and rerun focused tests, `NODE_ENV=test pnpm gate:war-room-v1`, `pnpm typecheck`, `pnpm build`, API read-only probes, and browser QA.
7. `visualqaagent` then `claudereviewer`: verify no visual drift, no safety regression, no final/release overclaim.
8. `releaseagent`: remains blocked by `t_124c7b12` unless DLV explicitly approves release packaging.

## Next card recommendations

1. Create: `Artdirector: Phase 10 GBA general/advisor unit asset contract` assigned to `artdirector`.
   - Output: `docs/status/art-direction/war-room-v1-phase10-general-advisor-unit-asset-contract.md`.
   - Scope: documentation-only, no image generation, no app/source/public asset edits.

2. Then create: `Prompt QA: Phase 10 general/advisor unit prompt readiness` assigned to `promptqaagent`, parented on the artdirector card.
   - Output: `docs/status/prompt-qa/war-room-v1-phase10-general-advisor-unit-prompt-qa.md`.
   - Scope: prompt/contract QA only, no generation and no app integration.

## Verdict

PASS: Phase 10 next-slice contract ready.

Exact recommendation: next card should be `artdirector`; the immediate dependent second card should be `promptqaagent`. Do not route to `assetcreator`, `technicalartist`, `claudearchitect`, `codexintegrator`, `visualqaagent`, or `releaseagent` until the contract and prompt QA gates pass.
