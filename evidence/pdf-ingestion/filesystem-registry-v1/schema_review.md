# Schema review

`canonical_knowledge_record.schema.json` is a closed Draft 2020-12 schema containing all
22 required fields, six allowed lifecycle statuses, ID/hash/version constraints, and
explicit sandbox/production environments. `filesystem_registry_config.schema.json` is
the closed configuration authority. JSON parsing and runtime valid/invalid tests pass.
